[package]
name = "rust-shell"
version = "0.1.0"
edition = "2024"

[dependencies]
nix = { version = "0.29.0", features = ["process", "signal", "unistd"] }
rustyline = "14"        # readline with history
thiserror = "2"         # error types